# Apps Overview

`apps/` contains deployable application workspaces built on top of Telva packages.

Current app workspace:

- `apps/www` — Next.js application (`telva-www`)

## Responsibilities

- Integrate package functionality into end-user product flows
- Handle app runtime concerns (routing, SSR, deployment setup)
- Provide product-facing UX on top of shared editor packages
